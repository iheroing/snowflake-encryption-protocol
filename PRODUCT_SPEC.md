# 雪花密语：产品发布规格

## 一句话定义

雪花密语是一封只能被主动揭开一次的雪信：写信人的文字在本地加密并凝结成独一无二的雪花，收信人揭开后，服务端保存的密文会在正常存储会话中立即对其他读取者失效。

它不是匿名通信工具、密码管理器或永久云笔记，也不承诺阻止收信人截图、录屏或复制已经显示的内容。

## 核心承诺

1. 明文只在写信人和收信人的浏览器内出现。
2. 服务端只保存密文、过期时间和必要的非秘密元数据，不保存解密密钥。
3. 解密密钥只存在于分享链接的 URL fragment 中，不进入 HTTP 请求。
4. 链接 fragment 另含与解密密钥独立的消费凭证；只知道公开路径 ID 不能销毁雪信。
5. 收信人必须明确点击「揭开雪信」才会消费消息，链接预览和预取不能触发销毁。
6. 正常存储会话中，同一雪信只有一次成功消费；未读雪信到期后自动失效。基础设施故障下的边界必须按 `SECURITY.md` 诚实披露。
7. 产品如实说明边界：运营方仍可能看到 IP、访问时间和密文大小；浏览器端加密仍信任当前网站交付的前端代码。

## V1 主流程

### 写信

1. 进入首页，点击「写一封雪信」。
2. 输入最多 500 个字符的心语，雪花随文字稳定变化。每次写信会话有独立的本地视觉私盐，因此即使正文相同，也会形成不同的雪花。
3. 选择未读有效期：1 小时、24 小时或 7 天，默认 24 小时。
4. 点击「凝结并封存」。水汽微粒聚拢、晶核形成、六向晶枝生长，并与封存声音及可用设备触觉同步；浏览器完成加密后，才上传密文。
5. 获得分享链接；可以复制、调用系统分享或主动显示本地生成的跨设备二维码。页面明确提醒写信人不要自行打开，也不要公开包含完整凭证的二维码，否则可能消耗唯一一次阅读机会。

### 收信

1. 打开链接后先看到密封雪花、来源提示和有效状态，正文尚未从服务端取回。
2. 点击「揭开雪信」触发一次性消费。
3. 服务端原子校验消费凭证、读取并删除密文；浏览器使用 fragment 中的密钥本地解密。
4. 正文出现，同时显示「这封雪信已经失效」；再次访问返回已读或过期状态。
5. 用户可以保存不含明文的雪花纪念卡，或主动将雪花视觉指纹收入本机标本馆。

### 雪花标本馆

1. 写信人在封存完成后、收信人在揭开后，均可主动收藏当前雪花；默认不收藏。
2. 收藏记录只含版本号、随机视觉签名、派生晶型、来源（寄出／收到）与本机收藏时间，不含正文、密文、密钥、分享链接或服务端雪信 ID。
3. 标本馆只读取版本化的 `snowflake:keepsakes:v1` 本地记录，不读取或自动迁移历史原型中的明文画廊数据。
4. 没有账户和跨设备同步；界面必须在收藏前说明数据仅在当前浏览器中，清理站点数据后会消失。

## 不进入 V1 主流程

- 永久云端保存、账号系统、跨设备历史记录。
- 默认保存明文的本地画廊；无明文且由用户主动确认的本机雪花标本馆允许进入主流程。
- 无法被技术保证的“内存覆写”“无法恢复”“防截图”。
- 在没有独立安全审计前宣称“军事级”“绝对安全”或“匿名”。

原型中的本地明文画廊不进入生产导航，也不会被新标本馆读取。

## 状态模型

| 状态 | 用户看到 | 服务端行为 |
| --- | --- | --- |
| `sealed` | 密封雪花、有效期、揭开按钮 | 元数据可查询，密文未消费 |
| `opening` | 与真实请求绑定的进度 | 执行原子 consume |
| `revealed` | 明文与已失效提示 | 密文已删除 |
| `gone` | 已被揭开或已过期 | 返回 `410`，不区分具体原因 |
| `invalid` | 链接无效 | 返回 `400/404`，不尝试解密 |
| `offline` | 网络不可用，可重试 | 不消费消息 |

## 发布验收门槛

- 25 个并发消费请求只能有 1 个成功，其余均为 `410`。
- 只提供 ID 或错误消费凭证的请求不能销毁密文。
- 链接预览、普通 GET、刷新 sealed 页面均不消费消息。
- 过期消息和二次读取不可恢复。
- 服务端日志、错误监控、分析工具不记录 URL 查询参数、fragment、明文或口令。
- 页面没有第三方运行时脚本；字体和图标本地托管；严格 CSP 无违规。
- 二维码仅在用户主动要求时于浏览器内生成；解码结果必须与完整分享链接逐字一致，并明确提示其等同阅读凭证。
- 标本馆持久化记录不得出现 `message`、密文、密钥、分享链接或可消费服务端内容的标识；同一视觉签名重复收藏必须去重。
- 构建产物不包含任何 API 密钥，完整依赖审计无 high/critical。
- 320、390、768、1280 像素宽度均无裁切或横向溢出。
- 全流程可仅用键盘完成；焦点可见；模态框管理焦点；支持 reduced motion、reduced transparency 和高对比度。
- 雪花引擎保持真实雪晶的六重对称，并至少覆盖星枝、蕨枝、六角板、针晶、分叉星五类不同生成语法；同一视觉签名稳定复现，至少 2,048 个固定样本不得出现视觉种子碰撞。
- 加密协议存在版本号、AAD、固定测试向量和跨实现验证。

## 体验语气

目标感受是平静、郑重、可信，而不是炫技。动效服务于「触碰—凝结—揭开—消散」四个因果瞬间；除环境氛围外不使用无目的循环动画。技术说明使用直白中文，把限制放在用户做决定之前。

## Snowflake Experience

- 心语输入时，视觉指纹持续演化；提交后文字降低实体感，转化为向晶核聚拢的冰粒。
- 凝结按「微粒聚合—晶核出现—六向生长—冰面掠光—封印完成」推进，不用无意义转圈替代过程。
- 声音、触觉和关键视觉帧同步触发；不支持触觉的设备自然降级，不影响主流程。
- 开启动效由晶型映射为绽放、融化或碎裂，正文以短暂去模糊方式出现。
- 后续关系主题只允许影响客户端颜色、晶型权重与开启动作；关系信息不得作为服务端明文元数据。若需要跨设备一致，应放在 URL fragment 或加密正文中。
- `prefers-reduced-motion` 下移除空间位移、旋转和碎裂，保留静态完成状态与必要文字反馈。
